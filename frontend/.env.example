VITE_API_URL=
# Persona "yo" de la demo (la que el editor de avatar sobreescribe y el default
# del selector de la consola). Vacío = `p_ana` en modo mock, `ana@empresa.com`
# contra la API de P3.
VITE_DEMO_USER_ID=
